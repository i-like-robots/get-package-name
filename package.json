{
  "name": "get-package-name",
  "version": "2.2.0",
  "description": "Get the name of a dependency from a module file path.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "npm run verify && npm run spec",
    "spec": "node --test --test-reporter spec",
    "lint": "eslint .",
    "format": "prettier --write '**/*.{js,json,yml}'",
    "verify": "npm run lint && npm run format",
    "prepublishOnly": "npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:i-like-robots/get-package-name.git"
  },
  "author": "Matt Hinchliffe",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/i-like-robots/get-package-name/issues"
  },
  "homepage": "https://github.com/i-like-robots/get-package-name",
  "devDependencies": {
    "eslint": "^8.43.0",
    "prettier": "^2.8.0"
  },
  "engines": {
    "node": ">= 12.0.0"
  }
}
